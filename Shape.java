public interface Shape {
    //Code sharing is not useful in this case because each class implements its behaviour differently

    // Area
    //Circle: Pi*R*R
    //Rectagnle= L*H
    //Triangle h*B/2
    // Interface can on;y conteain only method ,headers and class constants.



    public double CalculateArea();
  public double CalculatePerimeter();
}
